{
  "name": "@xmustard/pi-extension",
  "version": "0.1.0",
  "private": true,
  "description": "Pi extension exposing xMustard's nine tools over HTTP, with bounded evidence delivery and xmustard_expand paging.",
  "type": "module",
  "license": "UNLICENSED",
  "engines": {
    "node": ">=22.18.0"
  },
  "pi": {
    "extensions": [
      "./src/index.ts"
    ]
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "node --test test/unit.test.ts",
    "check": "npm run typecheck && npm test"
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "0.87.1",
    "@types/node": "24.3.1",
    "typescript": "5.9.2"
  }
}
